export interface DepthState {
    asks: any[];
    bids: any[];
    loading: boolean;
    error?: any;
}
export interface OrderBookState {
    asks: any[];
    bids: any[];
    loading: boolean;
    error?: any;
}
